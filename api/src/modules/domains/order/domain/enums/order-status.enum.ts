export enum OrderStatus {
  CANCELED = 'canceled',
  PENDING = 'pending',
  AWAITING_PAYMENT = 'awaiting_payment',
  EXPIRED = 'expired',
  PAID = 'paid',
  REFUNDED = 'refunded',
  COMPLETED = 'completed',
  ARCHIVED = 'archived'
}
